# run

TOP_DIR=$(cd $(dirname $0) && pwd)

source ${TOP_DIR}/config $*

docker run \
	--name $CONTAINER_NAME --privileged -d -t \
	-p 6080:6080 -p 2222:22 -p 4433:443 \
	-e UNIX_PWD=ubuntu -e VNC_PWD=ubuntu \
	-h localhost $IMAGE_NAME
